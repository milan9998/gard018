services:
  app:
    image: gard018-app:local
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_BASE_URL: http://localhost:3000

  scheduler:
    image: gard018-app:local

  caddy:
    profiles:
      - production
